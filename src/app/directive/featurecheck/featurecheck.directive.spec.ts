import { FeatureCheckDirective } from './featurecheck.directive';

describe('FeatureCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new FeatureCheckDirective();
    expect(directive).toBeTruthy();
  });
});
